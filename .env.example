# Anthropic API key (required)
ANTHROPIC_API_KEY=your_api_key_here

# Path to SQLite database file (optional, defaults to data/summaries.db)
# DB_PATH=data/summaries.db

# Log level (optional, defaults to INFO)
# LOG_LEVEL=INFO
